#include <iostream>
using namespace std;
/*
int main() {
	char cSymbol;
	double a, b, c, d;
	cout << "please input @, a, b, c, d: (seperated by Space)\n";
	cin >> cSymbol >> a >> b >> c >> d;
	if ((b == 0) || (d == 0))cout << "input error";
	switch (cSymbol) {
		case 43: cout << (a / b) + (c / d); break;
		case 45: cout << (a / b) - (c / d); break;
		case 42: cout << (a / b) * (c / d); break;
		case 47: {
			if (c == 0) {
				cout << "input error";
				break;
			}
			else {
				cout << (a / b) / (c / d);
				break;
			}				
		}
		default: cout << "input error";
	}

	return 0;
}//*/